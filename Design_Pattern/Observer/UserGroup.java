public class UserGroup implements Observer {
    private String name;

    public UserGroup(String name) {
        this.name = name;
    }

    @Override
    public void update(String message) {
        System.out.println(name + " received message: " + message);
    }
}

